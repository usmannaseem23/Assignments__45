// Assignment # 14
let guestList = ["Usman", "Umar", "Osama"];
// Invite each for dinner
guestList.forEach(guest => {
    console.log(`Dear ${guest} , You are cordially invited to dinner`);
});
export {};
